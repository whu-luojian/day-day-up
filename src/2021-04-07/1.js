/**
 * 算法 二维矩阵，左上角到右下角的最短路径和
 * 二维动态规划
[
    [4,6,1,3],
    [6,1,4,6],
    [9,3,1,8]
]
 */

/**
 * 实现 jsonp(url, cb)
 * jsonp('https://www.a.com/getdata', function(res) { console.log(res) })
 */

/**
 * vue 2 数组方法是怎么拦截的
 */

/**
 * 继承，静态属性继承
 */

/**
 * 怎么判断一个函数是在 new , 比如 new f
 * this instanceof f 判断不了 f.call(f) 这种情况，es6 new.target
 * 
 * new.target属性允许你检测函数或构造方法是否是通过new运算符被调用的。
 * 在通过new运算符被初始化的函数或构造方法中，new.target返回一个指向构造方法或函数的引用。
 * 在普通的函数调用中，new.target 的值是undefined。
 * https://developer.mozilla.org/zh-CN/docs/Web/JavaScript/Reference/Operators/new.target
 */

/**
 * vue 双向绑定原理
 */

/**
 * 找数组 a 的最大值
 * es6 es5 写法
 * Math.max(...a)
 * Math.max.apply(null, a)
 */

/**
 * 业务：基于对象和基于坑位的埋点管理方式
 * 
 * 基于坑位：
 * spm 层级需要全罗列
 * 页面结构变更导致相关埋点 id 变更，导致业务需要重新埋点
 * 埋点之间关联性弱，归因分析不好做
 * 
 * 基于对象
 * 对象到根对象上的OID链可作为对象位置的描述：_oid1|_oid2:_pos2|_oid3|_oid4|_oid5:_pos5
 */
